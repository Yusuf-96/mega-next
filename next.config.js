/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
    //   'bayut-production.s3.eu-central-1.amazonaws.com',
    ],
  },
};

module.exports = nextConfig
